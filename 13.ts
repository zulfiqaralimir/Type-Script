const vehicles: string[] = ["Honda motorcycle", "Tesla Model S", "Jeep Wrangler", "Ford Mustang"]; // Declare an array of strings that stores several examples of vehicles.

for (const vehicle of vehicles) {
  console.log(`I would like to own a ${vehicle}.`); // Print a statement for each item in the array.
}
