const names: string[] = ["Alice", "Bob", "Charlie", "David"]; // Declare an array of strings that stores the names of a few friends.
for (const name of names) {
  console.log(name); // Print each name in the array using a for...of loop.
}

