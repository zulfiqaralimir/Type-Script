const famousPerson: string = "Albert Einstein";
const quote: string = "Strive not to be a success, but rather to be of value.";
const message: string = `"${quote}" - ${famousPerson}`;

console.log(message);
