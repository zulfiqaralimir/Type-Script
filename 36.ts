function makeShirt(size: string, message: string): void {
    console.log(`You ordered a ${size} t-shirt with the message "${message}" printed on it.`);
  }
  
  makeShirt("medium", "Hello, World!"); // example call
  